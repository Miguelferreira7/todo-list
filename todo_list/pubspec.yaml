name: todo_list
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  get:
  get_storage:
  hive:
  hive_flutter:
  uuid:
  http:
  shared_preferences:


dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator:
  build_runner:


  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

